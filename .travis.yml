dist: xenial

language: java

jdk:
  - openjdk11

before_script:
  - chmod +x gradlew

script:
  - ./gradlew check
  - ./gradlew jacocoTestReport
  - ./gradlew sonarqube

after_success:
  - bash <(curl -s https://codecov.io/bash)